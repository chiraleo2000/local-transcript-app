--extra-index-url https://download.pytorch.org/whl/cu130
torch==2.11.0+cu130
torchvision==0.26.0+cu130
torchaudio==2.11.0+cu130
accelerate>=1.0
openvino==2026.1.0
optimum[openvino]>=1.17.0
# Gradio 4.44 + Starlette 1.x / FastAPI 0.115+ breaks Jinja2 TemplateResponse (HTTP 500).
fastapi>=0.112.0,<0.115.0
starlette>=0.37.2,<0.42.0
gradio==4.44.1
gradio_client==1.3.0
transformers>=4.57.0,<5
pyannote.audio==4.0.4
pedalboard
noisereduce>=3.0
librosa
soundfile
python-dotenv
psutil>=5.9
pywebview>=5.0

# --- Build / packaging (maintainer machine only) ----------------------------
# Install with: pip install -r requirements.txt pyinstaller
# pyinstaller>=6.10

# --- Optional accelerator backends (install per host platform) --------------
# Windows AMD/Intel GPU acceleration via DirectML (Windows only):
#   pip install torch-directml
# AMD ROCm acceleration (Linux only) requires a ROCm PyTorch build:
#   pip install --index-url https://download.pytorch.org/whl/rocm6.2 torch torchvision torchaudio

